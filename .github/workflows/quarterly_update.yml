- name: Commit and push
        run: |
          git config user.name "EDGAR Bot"
          git config user.email "actions@github.com"
          git add output/ docs/
          git diff --staged --quiet || git commit -m "Quarterly update — $(date +%Y-%m-%d)"
          git pull --rebase
          git push
